package com.classy.enums;

public enum SqlColumnTypes {
    // Numeric types
    TINYINT,
    SMALLINT,
    MEDIUMINT,
    INT,
    BIGINT,
    FLOAT,
    DOUBLE,
    DECIMAL,

    // Date and time types
    DATE,
    TIME,
    DATETIME,
    TIMESTAMP,
    YEAR,

    // String types
    CHAR,
    VARCHAR,
    BINARY,
    VARBINARY,
    TINYBLOB,
    BLOB,
    MEDIUMBLOB,
    LONGBLOB,
    TINYTEXT,
    TEXT,
    MEDIUMTEXT,
    LONGTEXT,

    // Enumeration types
    ENUM,
    SET,
    GEOMETRY
}
